<ui:FluentWindow x:Class="DevGearbox.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="DevGearbox" 
        Height="750" Width="1300"
        MinHeight="600" MinWidth="1000"
        WindowStartupLocation="CenterScreen"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica">
    <Window.Resources>
        <Style x:Key="NavItemStyle" TargetType="RadioButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#B0B0B0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,12"/>
            <Setter Property="Margin" Value="4,2"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{TemplateBinding Tag}" FontSize="20" Margin="0,0,12,0" VerticalAlignment="Center" Foreground="{TemplateBinding Foreground}"/>
                                <TextBlock Grid.Column="1" Text="{TemplateBinding Content}" VerticalAlignment="Center" FontWeight="Medium" Foreground="{TemplateBinding Foreground}"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#2A2A2A"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Background" Value="#0078D4"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ui:TitleBar Grid.Row="0" Title="DevGearbox" />
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" Background="#1E1E1E" BorderBrush="#2D2D30" BorderThickness="0,0,1,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <StackPanel Grid.Row="0" Margin="16,20,16,16">
                        <TextBlock Text="🛠️ DevGearbox" FontSize="22" FontWeight="Bold" Foreground="White" Margin="0,0,0,4"/>
                        <TextBlock Text="15 Developer Tools" FontSize="11" Foreground="#808080"/>
                    </StackPanel>
                    <Border Grid.Row="1" Background="#252526" BorderBrush="#3E3E42" BorderThickness="1" CornerRadius="4" Margin="12,0,12,16">
                        <TextBox Name="SearchBox" Background="Transparent" BorderThickness="0" Foreground="White" Padding="10,8" FontSize="12" TextChanged="SearchBox_TextChanged"/>
                    </Border>
                    <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                        <StackPanel Name="NavigationPanel" Margin="8,0,8,16"/>
                    </ScrollViewer>
                    <Border Grid.Row="3" Background="#252526" Padding="12,10" Margin="8,0,8,8" CornerRadius="4">
                        <StackPanel>
                            <TextBlock Text="🔒 100% Offline" FontSize="11" FontWeight="SemiBold" Foreground="#4EC9B0" Margin="0,0,0,4"/>
                            <TextBlock Text="No data leaves your machine" FontSize="10" Foreground="#808080"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>
            <Border Grid.Column="1" Background="#252526">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Border Grid.Row="0" Background="#1E1E1E" BorderBrush="#2D2D30" BorderThickness="0,0,0,1" Padding="24,16">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Name="ToolIconText" Text="🛠️" FontSize="28" Margin="0,0,12,0" VerticalAlignment="Center"/>
                            <StackPanel>
                                <TextBlock Name="ToolNameText" Text="Select a tool" FontSize="20" FontWeight="SemiBold" Foreground="White"/>
                                <TextBlock Name="ToolDescriptionText" Text="Choose a tool from the sidebar" FontSize="12" Foreground="#808080" Margin="0,2,0,0"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                    <ContentControl Grid.Row="1" Name="ToolContentControl"/>
                </Grid>
            </Border>
        </Grid>
        <Border Grid.Row="2" Background="#007ACC" Padding="16,6">
            <Grid>
                <TextBlock Name="StatusText" Text="Ready" Foreground="White" FontSize="11" VerticalAlignment="Center"/>
                <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                    <TextBlock Text="v0.0.1" Foreground="White" FontSize="10" Margin="0,0,12,0" Opacity="0.8"/>
                    <TextBlock Text="DevGearbox © 2025" Foreground="White" FontSize="10" Opacity="0.8"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</ui:FluentWindow>

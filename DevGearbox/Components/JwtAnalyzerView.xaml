<UserControl x:Class="DevGearbox.Components.JwtAnalyzerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">
            <TextBlock Text="JWT Token Analyzer" Style="{StaticResource HeaderStyle}"/>
            <TextBlock Text="Decode and analyze JWT tokens" Foreground="#9CDCFE" Margin="0,0,0,15"/>
            <Label Content="JWT Token:"/>
            <TextBox Name="JwtInput" Height="120"/>
            <Button Content="Analyze JWT" Click="AnalyzeJwt_Click" Margin="0,10" HorizontalAlignment="Left"/>
            <Label Content="Analysis Result:"/>
            <TextBox Name="JwtOutput" Height="350" IsReadOnly="True"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>

<UserControl x:Class="DevGearbox.Components.FileEncodingDetectorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="File Encoding Detector" Style="{StaticResource HeaderStyle}"/>
            <TextBlock Text="Trascina un file per scoprire rapidamente il suo encoding" Opacity="0.8"/>
        </StackPanel>

        <ui:InfoBar Grid.Row="1"
                    Name="StatusInfoBar"
                    Title="Trascina un file nell'area sottostante o scegli un file"
                    Severity="Informational"
                    IsOpen="True"
                    IsClosable="False"
                    Margin="0,0,0,15"/>

        <ui:Card Grid.Row="2" Padding="20" AllowDrop="True"
                 Drop="DropArea_Drop"
                 DragEnter="DropArea_DragEnter"
                 DragLeave="DropArea_DragLeave">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Border Name="DropBorder"
                        BorderBrush="#3E3E42"
                        BorderThickness="2"
                        CornerRadius="8"
                        Background="#252526"
                        Padding="20">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock Text="📂" FontSize="32" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                        <TextBlock Text="Trascina qui un file per rilevare l'encoding" 
                                   FontSize="14" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,0,0,4"/>
                        <TextBlock Name="SelectedFileText"
                                   Text="Nessun file selezionato"
                                   Opacity="0.7"
                                   HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <ui:Button Content="Scegli file..."
                                   Width="140"
                                   Click="BrowseFile_Click"
                                   Icon="{ui:SymbolIcon FolderOpen24}"
                                   Margin="0,10,0,0"/>
                    </StackPanel>
                </Border>
                <ui:Card Grid.Row="1" Margin="0,15,0,0" Padding="15">
                    <StackPanel>
                        <TextBlock Text="Encoding rilevato" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <TextBlock Name="DetectedEncodingText"
                                   Text="—"
                                   FontFamily="Consolas"
                                   FontSize="13"/>
                    </StackPanel>
                </ui:Card>
            </Grid>
        </ui:Card>

        <ui:Card Grid.Row="3" Padding="15" Margin="0,15,0,0">
            <TextBlock FontSize="12" Opacity="0.8" TextWrapping="Wrap">
                Suggerimento: il tool riconosce BOM comuni (UTF-8, UTF-16/32) e tenta un rilevamento euristico per ASCII, UTF-8 e Windows-1252.
            </TextBlock>
        </ui:Card>
    </Grid>
</UserControl>

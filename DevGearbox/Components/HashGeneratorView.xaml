<UserControl x:Class="DevGearbox.Components.HashGeneratorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">
            <TextBlock Text="Hash Generator" Style="{StaticResource HeaderStyle}"/>
            <TextBlock Text="Generate cryptographic hashes from text" Foreground="#9CDCFE" Margin="0,0,0,15"/>
            <Label Content="Input Text:"/>
            <TextBox Name="HashInput" Height="100"/>
            <Label Content="Hash Algorithms:" Margin="0,15,0,5"/>
            <StackPanel Orientation="Horizontal" Margin="0,5">
                <Button Content="MD5" Click="GenerateMD5_Click" Margin="0,0,10,0"/>
                <Button Content="SHA-1" Click="GenerateSHA1_Click" Margin="0,0,10,0"/>
                <Button Content="SHA-256" Click="GenerateSHA256_Click" Margin="0,0,10,0"/>
                <Button Content="SHA-512" Click="GenerateSHA512_Click"/>
            </StackPanel>
            <Label Content="Hash Output:"/>
            <TextBox Name="HashOutput" Height="100" IsReadOnly="True"/>
            <TextBlock Margin="0,10,0,0" Foreground="#CE9178" FontSize="11">
                <Run Text="⚠ Note: MD5 and SHA-1 are cryptographically broken and should not be used for security purposes."/>
            </TextBlock>
        </StackPanel>
    </ScrollViewer>
</UserControl>

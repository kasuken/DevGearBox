<UserControl x:Class="DevGearbox.Components.FormattersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">
            <!-- JSON Formatter -->
            <ui:Card Padding="20" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="JSON Formatter" Style="{StaticResource HeaderStyle}"/>
                    <Label Content="Input JSON:"/>
                    <ui:TextBox Name="JsonInput" MinHeight="200" FontFamily="Consolas" TextWrapping="Wrap" AcceptsReturn="True"/>
                    <StackPanel Orientation="Horizontal" Margin="0,10">
                        <ui:Button Content="Format (Pretty)" Click="FormatJson_Click" 
                                   Icon="{ui:SymbolIcon TextGrammarCheckmark24}"
                                   Appearance="Primary"
                                   Margin="0,0,10,0"/>
                        <ui:Button Content="Minify" Click="MinifyJson_Click"
                                   Icon="{ui:SymbolIcon TextCollapse24}"/>
                    </StackPanel>
                    <Label Content="Output:"/>
                    <ui:TextBox Name="JsonOutput" MinHeight="200" IsReadOnly="True" FontFamily="Consolas" TextWrapping="Wrap" AcceptsReturn="True"/>
                </StackPanel>
            </ui:Card>
            
            <!-- XML Formatter -->
            <ui:Card Padding="20">
                <StackPanel>
                    <TextBlock Text="XML Formatter" Style="{StaticResource HeaderStyle}"/>
                    <Label Content="Input XML:"/>
                    <ui:TextBox Name="XmlInput" MinHeight="200" FontFamily="Consolas" TextWrapping="Wrap" AcceptsReturn="True"/>
                    <StackPanel Orientation="Horizontal" Margin="0,10">
                        <ui:Button Content="Format (Pretty)" Click="FormatXml_Click"
                                   Icon="{ui:SymbolIcon TextGrammarCheckmark24}"
                                   Appearance="Primary"
                                   Margin="0,0,10,0"/>
                        <ui:Button Content="Minify" Click="MinifyXml_Click"
                                   Icon="{ui:SymbolIcon TextCollapse24}"/>
                    </StackPanel>
                    <Label Content="Output:"/>
                    <ui:TextBox Name="XmlOutput" MinHeight="200" IsReadOnly="True" FontFamily="Consolas" TextWrapping="Wrap" AcceptsReturn="True"/>
                </StackPanel>
            </ui:Card>
        </StackPanel>
    </ScrollViewer>
</UserControl>
